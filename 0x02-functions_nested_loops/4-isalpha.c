#include "main.h"
/**
 * _isalpha - this function checks if charater is in lower case or upper
 *
 * @c: is the value for the argument to be passed
 * Return: 0 in excess
 */
int _isaplpha(int c)
{
	if (c >= 'a'; && c <= 'z') || (c >= 'A' && c <= 'Z'))
	{
		return (1);
	}
	else
		return (0);
}
